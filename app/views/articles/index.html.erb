<head> 
  <title>Messages</title>
</head>

<section class="row-alt mt-5">
  <div class="lead container">
    <p>Be kind and leave a message<%= " (login required)" if !logged_in? %>.</p>
    <% if logged_in? %>
      <%= link_to "New message", new_article_path, class: "btn btn-success" %>
    <% end %>
  </div>
</section>

<div class="flickr_pagination mt-4">
  <%= will_paginate @articles, :container => false %>
</div>
<%= render 'article' %>
<div class="flickr_pagination mb-4">
  <%= will_paginate @articles, :container => false %>
</div>